/**
 * Hardware Config Feature Module
 * Provides motor library and hardware configuration capabilities
 */

// Re-export from shared data layer for backward compatibility
export { DEFAULT_MOTOR_LIBRARY } from '@/shared/data/motorLibrary';
